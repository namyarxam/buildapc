const React = require('react');

const StepOne = React.createClass({
  render: function() {
    return (
      <div className="step1">
        <p><span className="bold">THE PROCESSOR:</span> The processor is all about the speed of your computer. A faster processor will be able to run more programs at once.</p>
      </div>
    )
  }
})

module.exports = StepOne;